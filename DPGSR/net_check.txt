 
要开始，请键入 doc。
有关产品信息，请访问 www.mathworks.com。
 
resnet50: resnet50 需要 Deep Learning Toolbox Model for ResNet-50 Network 支持包以使用预训练的权重。要安装此支持包，请使用<a href="matlab: matlab.addons.supportpackage.internal.explorer.showSupportPackages('RESNET50', 'tripwire')">附加功能资源管理器</a>。要使用未训练的层，请使用 resnet50('Weights','none')，这不需要支持包。
vgg16: vgg16 需要 Deep Learning Toolbox Model for VGG-16 Network 支持包以使用预训练的权重。要安装此支持包，请使用<a href="matlab: matlab.addons.supportpackage.internal.explorer.showSupportPackages('VGG16', 'tripwire')">附加功能资源管理器</a>。要使用未训练的层，请使用 vgg16('Weights','none')，这不需要支持包。
googlenet: googlenet 需要 Deep Learning Toolbox Model for GoogLeNet Network 支持包以使用预训练的权重。要安装此支持包，请使用<a href="matlab: matlab.addons.supportpackage.internal.explorer.showSupportPackages('GOOGLENET', 'tripwire')">附加功能资源管理器</a>。要使用未训练的层，请使用 googlenet('Weights','none')，这不需要支持包。
alexnet: alexnet 需要 Deep Learning Toolbox Model for AlexNet Network 支持包以使用预训练的权重。要安装此支持包，请使用<a href="matlab: matlab.addons.supportpackage.internal.explorer.showSupportPackages('ALEXNET', 'tripwire')">附加功能资源管理器</a>。要使用未训练的层，请使用 alexnet('Weights','none')，这不需要支持包。
inceptionv3: inceptionv3 需要 Deep Learning Toolbox Model for Inception-v3 Network 支持包以使用预训练的权重。要安装此支持包，请使用<a href="matlab: matlab.addons.supportpackage.internal.explorer.showSupportPackages('INCEPTIONV3', 'tripwire')">附加功能资源管理器</a>。要使用未训练的层，请使用 inceptionv3('Weights','none')，这不需要支持包。
mobilenetv2: mobilenetv2 需要 Deep Learning Toolbox Model for MobileNet-v2 Network 支持包以使用预训练的权重。要安装此支持包，请使用<a href="matlab: matlab.addons.supportpackage.internal.explorer.showSupportPackages('MOBILENET_V2', 'tripwire')">附加功能资源管理器</a>。要使用未训练的层，请使用 mobilenetv2('Weights','none')，这不需要支持包。
